<div class="util-head" style="padding-bottom:90px">
  <p class="eyebrow">{{ shop.name }} · London</p>
  <h1 class="sec-title display" style="font-size:clamp(44px,7vw,90px)">Lost the <em style="font-style:italic;color:var(--accent)">thread</em></h1>
  <p class="coll-desc">The page you're looking for isn't here — but beautiful things are waiting just one step back.</p>
  <p style="margin-top:34px"><a class="btn" href="{{ routes.root_url }}">Return home</a></p>
</div>

{% schema %}
{
  "name": "404",
  "settings": []
}
{% endschema %}
