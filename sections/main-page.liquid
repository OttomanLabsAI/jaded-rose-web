<div class="article-wrap">
  <p class="eyebrow">{{ shop.name }}</p>
  <h1 class="article-title display">{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</div>

{% schema %}
{
  "name": "Page",
  "settings": []
}
{% endschema %}
